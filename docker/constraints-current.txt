# Alternate pin set: current upstream resolution (recorded 17 September 2026 on
# CPython 3.13 / Windows). Use this to test against newer NumPy, accepting that
# numeric artifacts may differ in the last bits from the archived experiments.
#
#   docker build --build-arg CONSTRAINTS=docker/constraints-current.txt -t istana-rl .

numpy==2.5.3
gymnasium==1.3.0
pettingzoo==1.27.0
pytest==8.4.2

cloudpickle==3.1.2
Farama-Notifications==0.0.6
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
typing_extensions==4.16.0
