# Pinned dependency set for reproducible Blue Team RL runs.
#
# numpy is pinned to the version recorded by the published experiments
# (see RL/BlueTeam/TEMPORAL_RESULTS.md: "Python 3.11.9, NumPy 2.4.6", and the
# ranking pilot's Linux verification on "Python 3.12.3, NumPy 2.4.6"). Matching
# it is what makes the archived numeric evidence reproducible rather than merely
# re-runnable. Note that RL/BlueTeam/BALANCED_RESULTS.md already documents
# last-bit (~5.7e-14) float differences between Windows and Linux; those change
# unrounded input hashes without changing any sensor choice or action count.
#
# The remaining pins are a resolved, self-consistent set for Python 3.11.
# To build against current upstream instead, use constraints-current.txt:
#   docker build --build-arg CONSTRAINTS=docker/constraints-current.txt -t istana-rl .

numpy==2.4.6
gymnasium==1.3.0
pettingzoo==1.27.0
pytest==8.4.2
Pillow==12.3.0
imageio-ffmpeg==0.6.0

# Transitive dependencies, pinned so the image is byte-stable across rebuilds.
cloudpickle==3.1.2
Farama-Notifications==0.0.6
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
typing_extensions==4.16.0
